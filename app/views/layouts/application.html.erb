<!DOCTYPE html>
<html>
    <head>
        <title><%= full_title(yield(:title)) %></title>
        <%= csrf_meta_tags %>

        <link href="https://fonts.googleapis.com/icon?family=Material+Icons"rel="stylesheet">
        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <body>
        <header class="header-main">
            <p><%= yield :breadcrum %></p>
            <div class="header-options">
                <%= link_to logout_path, method: :delete do %>
                    <i class="material-icons">power_settings_new</i>
                <% end %>
            </div>
        </header>
        <div id="nav-side" class="nav-side">
            <div class="side-logo">GTAPROS</div>
            <nav>
                <dl>
                    <% if current_user.categoria == 0 %>
                        <dt>Empleados</dt>
                        <dt>Nuevo proyecto</dt>
                        <dt>Asignar responsable proyecto</dt>
                        <dt>Cargar datos iniciales</dt>
                    <% else %>
                        <dt>Proyectos</dt>
                        <dt>Calendario</dt>
                    <% end %>
                </dl>
            </nav>
            <div class="nav-footer">
                <p>Grupo 07</p>
                <ul>
                    <li>Pablo Carrascal Muñoz</li>
                    <li>Pablo Garcia Sanz</li>
                    <li>Sergio Garcia Villanueva</li>
                    <li>Rodrigo Nuñez Paul</li>
                </ul>
            </div>
        </div>
        <div class="content">
            <%= yield %>
        </div>
    </body>
</html>
